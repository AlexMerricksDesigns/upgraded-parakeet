.next/
node_modules/
out/

.env
.env*.local
.env.example

content/crypto/.probe-output/

# Local asset catalog DB (optional; CSV is source of truth in repo)
data/catalog.db
data/assets-index.json

npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Cold-storage ingest — not used at runtime (see PROJECT_LAYOUT.md)
/0consolidating previous attempts content ingest folder/
.consolidating previous attempts content ingest folder/
# OS / editor noise
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini
# Optional local tooling
.venv/
.vercel/
.eslintcache
# IDE (optional — omit if you want shared workspace settings)
.vscode/
.idea/
# Public assets folder
public_originals/
public/
