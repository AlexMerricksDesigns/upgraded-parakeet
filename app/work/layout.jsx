export const metadata = {
  title: "Work",
  description: "Explorer — portfolio projects grouped by discipline.",
};

export default function WorkLayout({ children }) {
  return <div data-lane="work">{children}</div>;
}
